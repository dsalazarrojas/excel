// excel.gic.mx (this storefront) and api.excel.gic.mx (the Goods API,
// a custom domain on the shared onepageapps-bridge Worker) are separate
// origins, so this must stay set.
window.GOODS_API_BASE = window.GOODS_API_BASE || 'https://api.excel.gic.mx';
